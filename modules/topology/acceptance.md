# Topology Acceptance

- [ ] Contract contains no HTTP route or Runtime implementation detail.
- [ ] Every mutation returns the resulting Layer generation.
- [ ] Personal/System authority is enforced through public actor/Peer contracts.
- [ ] Same-Layer references are enforced by domain validation and composite FK.
- [ ] Node remains persistent intent when Peer presence later changes.
- [ ] Service ports normalize deterministically.
- [ ] `selector: all` remains semantic and source-only.
- [ ] Resource deletion returns structured resource_in_use.
- [ ] FindLayersReferencingPeers returns exactly Layers with live Node references for the supplied Peer set.
- [ ] DeleteRetiredLayer is callable only through composition after Runtime eligibility.
